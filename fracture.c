#include <stdio.h>
#include <math.h>

int main(){
    printf("Enter the x value for the first point. \n")
    double calculateDistance();
        px1=0;
        px2=0;
        py1=0;
        py2=0;
        scanf("%d", &px1);
        printf("Point #1 entered: %d", px1);
    return 0; 
}